
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to any signed-in user for all collections.
    // This is a temporary, permissive rule for debugging purposes.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
